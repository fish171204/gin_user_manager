lifecycle: client --> routes --> handler --> service --> repositcky --> model

init : 
        1. user (model)
        2. repo = user_repository(user)
        3. service user_service(repo)
        4. handler = user_handler(service)
        5. routes = user_routes(handler)
